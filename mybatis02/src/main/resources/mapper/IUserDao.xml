<?xml version="1.0" encoding="utf-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTO Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.kfm.mybatis02.dao.IUserDao">

    <select id="selectAll" resultType="com.kfm.mybatis02.model.UserModel">
        select * from users
    </select>
    <select id="selectByUserModel" resultType="com.kfm.mybatis02.model.UserModel">
        select * from users
        <where>
            <if test="username!= null">
                and username = #{username}
            </if>
            <if test="password!= null">
                and password = #{password}
            </if>

            <if test="name!=null">
                and name = #{name}
            </if>
            <if test="phone!=null">
                and phone = #{phone}
            </if>
            <if test="email!=null">
                and email = #{email}
            </if>
            <if test="avatar!=null">
                and avatar = #{avatar}
            </if>
            <if test="state!=null">
                and state = #{state}
            </if>
        </where>
    </select>
    <insert id="insertUserModel" parameterType="com.kfm.mybatis02.model.UserModel">
        insert into users (id,username,password,name) values (#{id},#{username},#{password},#{name})
    </insert>
</mapper>