spring:
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://localhost:3306/db_business?useSSL=false&serverTimezone=UTC
    username: root
    password: 1234
  devtools:
    restart:
      enabled: true

logging:
  level:
    com.kfm.plus.mapper: debug


mybatis-plus:
  mapper-locations: classpath:mapper/*.xml
  configuration:
    map-underscore-to-camel-case: true
